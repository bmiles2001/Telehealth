namespace Telehealth.UI.Features.Scheduling;

public static partial class Schedule
{
	// Moved the relevant queries to separate files
	public const string ProviderContentPlaceholder = nameof(ProviderContentPlaceholder);
	public const string ProviderTextFieldName = nameof(ProviderDetails.ProviderName);
}
